---
interface Props {
    number: number;
}
const { number } = Astro.props;
---
<div class="bg-[#FF3366] text-white min-w-12 min-h-12 flex items-center justify-center rounded-md font-bold">
  <span>{number}</span>
</div>
